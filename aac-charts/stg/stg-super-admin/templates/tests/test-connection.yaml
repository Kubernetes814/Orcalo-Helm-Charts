apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stg-super-admin.fullname" . }}-test-connection"
  labels:
    {{- include "stg-super-admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stg-super-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
