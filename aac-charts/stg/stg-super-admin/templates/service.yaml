apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-super-admin.fullname" . }}
  labels:
    {{- include "stg-super-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-super-admin.selectorLabels" . | nindent 4 }}
