apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-air-service.fullname" . }}
  labels:
    {{- include "stg-air-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-air-service.selectorLabels" . | nindent 4 }}
