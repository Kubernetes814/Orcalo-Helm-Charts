apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-common-feature.fullname" . }}
  labels:
    {{- include "stg-common-feature.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-common-feature.selectorLabels" . | nindent 4 }}
