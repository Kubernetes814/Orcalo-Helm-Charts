apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stg-operations.fullname" . }}-test-connection"
  labels:
    {{- include "stg-operations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stg-operations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
