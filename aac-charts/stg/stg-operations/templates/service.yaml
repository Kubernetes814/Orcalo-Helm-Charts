apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-operations.fullname" . }}
  labels:
    {{- include "stg-operations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-operations.selectorLabels" . | nindent 4 }}
