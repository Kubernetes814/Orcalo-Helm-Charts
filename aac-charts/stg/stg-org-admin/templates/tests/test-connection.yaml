apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stg-org-admin.fullname" . }}-test-connection"
  labels:
    {{- include "stg-org-admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stg-org-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
