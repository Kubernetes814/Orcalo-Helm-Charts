apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-org-admin.fullname" . }}
  labels:
    {{- include "stg-org-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-org-admin.selectorLabels" . | nindent 4 }}
