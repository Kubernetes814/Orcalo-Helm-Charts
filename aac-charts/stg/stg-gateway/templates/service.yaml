apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-gateway.fullname" . }}
  labels:
    {{- include "stg-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-gateway.selectorLabels" . | nindent 4 }}
