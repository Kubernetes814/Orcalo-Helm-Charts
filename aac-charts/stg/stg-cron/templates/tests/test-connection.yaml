apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stg-cron.fullname" . }}-test-connection"
  labels:
    {{- include "stg-cron.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stg-cron.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
