apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-loyalty-program.fullname" . }}
  labels:
    {{- include "stg-loyalty-program.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-loyalty-program.selectorLabels" . | nindent 4 }}
