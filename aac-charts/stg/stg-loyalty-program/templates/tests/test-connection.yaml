apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stg-loyalty-program.fullname" . }}-test-connection"
  labels:
    {{- include "stg-loyalty-program.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stg-loyalty-program.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
