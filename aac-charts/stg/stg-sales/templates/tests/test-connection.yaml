apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stg-sales.fullname" . }}-test-connection"
  labels:
    {{- include "stg-sales.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stg-sales.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
