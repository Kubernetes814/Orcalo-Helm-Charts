apiVersion: v1
kind: Service
metadata:
  name: {{ include "stg-sales.fullname" . }}
  labels:
    {{- include "stg-sales.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stg-sales.selectorLabels" . | nindent 4 }}
