apiVersion: v1
kind: Service
metadata:
  name: {{ include "prod-gateway.fullname" . }}
  labels:
    {{- include "prod-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prod-gateway.selectorLabels" . | nindent 4 }}
