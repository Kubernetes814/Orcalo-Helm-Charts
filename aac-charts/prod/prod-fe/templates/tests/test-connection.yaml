apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prod-fe.fullname" . }}-test-connection"
  labels:
    {{- include "prod-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prod-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
