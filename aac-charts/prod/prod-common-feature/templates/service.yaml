apiVersion: v1
kind: Service
metadata:
  name: {{ include "prod-common-feature.fullname" . }}
  labels:
    {{- include "prod-common-feature.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prod-common-feature.selectorLabels" . | nindent 4 }}
