apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prod-user-account.fullname" . }}-test-connection"
  labels:
    {{- include "prod-user-account.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prod-user-account.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
