apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prod-operations.fullname" . }}-test-connection"
  labels:
    {{- include "prod-operations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prod-operations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
