apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prod-cron.fullname" . }}-test-connection"
  labels:
    {{- include "prod-cron.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prod-cron.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
