apiVersion: v1
kind: Service
metadata:
  name: {{ include "prod-super-admin.fullname" . }}
  labels:
    {{- include "prod-super-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prod-super-admin.selectorLabels" . | nindent 4 }}
