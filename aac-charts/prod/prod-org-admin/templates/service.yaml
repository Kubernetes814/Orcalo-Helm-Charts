apiVersion: v1
kind: Service
metadata:
  name: {{ include "prod-org-admin.fullname" . }}
  labels:
    {{- include "prod-org-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prod-org-admin.selectorLabels" . | nindent 4 }}
