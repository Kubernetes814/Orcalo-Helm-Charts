apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-air-service.fullname" . }}
  labels:
    {{- include "dev-air-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-air-service.selectorLabels" . | nindent 4 }}
