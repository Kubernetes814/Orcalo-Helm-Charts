apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-marketing.fullname" . }}
  labels:
    {{- include "dev-marketing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-marketing.selectorLabels" . | nindent 4 }}
