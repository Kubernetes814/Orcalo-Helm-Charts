apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-common-feature.fullname" . }}
  labels:
    {{- include "dev-common-feature.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-common-feature.selectorLabels" . | nindent 4 }}
