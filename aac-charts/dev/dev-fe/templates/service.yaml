apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-fe.fullname" . }}
  labels:
    {{- include "dev-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-fe.selectorLabels" . | nindent 4 }}
