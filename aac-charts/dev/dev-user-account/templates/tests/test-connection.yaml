apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-user-account.fullname" . }}-test-connection"
  labels:
    {{- include "dev-user-account.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-user-account.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
