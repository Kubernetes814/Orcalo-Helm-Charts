apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-user-account.fullname" . }}
  labels:
    {{- include "dev-user-account.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-user-account.selectorLabels" . | nindent 4 }}
