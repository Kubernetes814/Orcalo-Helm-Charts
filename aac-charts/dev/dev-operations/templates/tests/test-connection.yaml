apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-operations.fullname" . }}-test-connection"
  labels:
    {{- include "dev-operations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-operations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
