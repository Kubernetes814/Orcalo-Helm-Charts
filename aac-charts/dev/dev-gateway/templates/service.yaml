apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-gateway.fullname" . }}
  labels:
    {{- include "dev-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-gateway.selectorLabels" . | nindent 4 }}
