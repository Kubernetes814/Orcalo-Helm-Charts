apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "dev-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
