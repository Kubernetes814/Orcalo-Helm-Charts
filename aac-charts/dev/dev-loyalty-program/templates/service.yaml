apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-loyalty-program.fullname" . }}
  labels:
    {{- include "dev-loyalty-program.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-loyalty-program.selectorLabels" . | nindent 4 }}
