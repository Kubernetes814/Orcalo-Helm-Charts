apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-sales.fullname" . }}-test-connection"
  labels:
    {{- include "dev-sales.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-sales.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
