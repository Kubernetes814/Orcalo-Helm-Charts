apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-sales.fullname" . }}
  labels:
    {{- include "dev-sales.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-sales.selectorLabels" . | nindent 4 }}
