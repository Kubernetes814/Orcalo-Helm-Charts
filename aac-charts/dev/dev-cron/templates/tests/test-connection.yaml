apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-cron.fullname" . }}-test-connection"
  labels:
    {{- include "dev-cron.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-cron.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
