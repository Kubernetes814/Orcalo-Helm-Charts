apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-cron.fullname" . }}
  labels:
    {{- include "dev-cron.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-cron.selectorLabels" . | nindent 4 }}
